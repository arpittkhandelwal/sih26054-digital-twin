# ─── Mosquitto broker config (hackathon / local dev only) ─────────────
# WARNING: anonymous access enabled — do NOT use in production.

listener 1883
allow_anonymous true

# Persistence (optional, helps debugging between restarts)
persistence true
persistence_location /mosquitto/data/

# Logging
log_dest stdout
log_type all
